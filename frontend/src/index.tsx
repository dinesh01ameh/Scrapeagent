import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root') as HTMLElement);

console.log('🚀 SwissKnife AI Scraper - COMPLETE Authentication Bypass');
console.log('✅ Zero authentication logic - Pure routing only');
console.log('🔧 No API calls - No auth validation - No redirects');
console.log('📝 All features accessible without any authentication barriers');

// Render without StrictMode to prevent development issues
root.render(<App />);
